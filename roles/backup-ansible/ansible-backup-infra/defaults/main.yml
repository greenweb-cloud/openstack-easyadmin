---
# defaults file for ansible-backup-infra
float_ip: "FLOAT_IP"

# List of NFS shares

# NFS version, default 4
nfs_version: 4

# Default NFS4 mount options
nfs_mount_opts: rsize=8192,wsize=8192,timeo=14,intr

nfs_share_mounts:
- path: /Backup 
  location: IP:SHARED_DIR
